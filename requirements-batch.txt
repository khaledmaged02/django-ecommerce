# Extra dependencies added for Requirements 4 (Batch) and 5 (Load Distribution).
# Install with:  pip install -r requirements-batch.txt
celery>=5.3
redis>=5.0
django-celery-beat>=2.5
django-celery-results>=2.5

# Optional — used only by the optional stress-test script in the run guide.
# locust>=2.20
